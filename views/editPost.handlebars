<!-- CREATE A POST FORM -->
<form id="edit-post-form" name="{{post.id}}">
    <div class="post-header">
        <h2>Edit Your Post</h2>
    </div>
    <div class="post-content">
        <div>
            <label>Post Title</label>
            <textarea class="textarea" placeholder="title" id="post-title">{{post.title}}</textarea>
            <label>Content</label>
            <textarea class="textarea" placeholder="text" id="post-text">{{post.content}}</textarea>
        </div>
        <button class="btn-generic" type="submit" id="save">Update</button>
        <button class="btn-generic" type="reset" id="delete">Delete</button>
    </div>
</form>

<script src="/js/editPost.js"></script>